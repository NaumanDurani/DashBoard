module.exports = {
  // Specify the input and output directories/files
  files: {
    input: 'style/main.scss',
    output: 'style/output.css',
  },
  // Optional: Set the output style (nested, expanded, compact, compressed)
  outputStyle: 'compressed',
  // Optional: Enable source maps
  sourceMap: true,
};
